{
    "reasoner": {
        "100": "When handling a customer-service request with multiple sub-tasks and conditional logic, first enumerate every sub-request and every \"if X then Y else Z\" branch explicitly before acting, then stress-test each branch by asking what happens if the preferred variant is unavailable, if an order is already delivered rather than pending, or if authentication data is missing. Only after confirming the full plan against domain policies (same-item-type exchanges, pending-only modifications, name+zip authentication when email is unknown) should you commit to tool calls, and never silently default to the primary option when a fallback condition is triggered.",
        "101": "Before executing any tool call in a multi-step customer-service scenario, run an explicit verification pass: restate each sub-request, identify which policy constraints apply (authentication method, order status, item-variant matching), and check whether you have actually queried the catalog or order system to confirm availability rather than assuming it. If any conditional fallback was specified, verify which branch is active by citing the retrieved evidence, and flag missing data as a question to the customer instead of guessing.",
        "102": "When reasoning through a multi-part customer-service scenario, construct a numbered proof-style derivation in which each step cites the specific policy or retrieved data that licenses it: step 1 establishes identity via name+zip (since email is unknown), step 2 enumerates every sub-request and conditional branch as separate lemmas, and step 3 discharges each lemma only after tool evidence confirms preconditions like pending status, variant availability, or same-item-type matching. Treat any unverified premise as a blocking question to the customer rather than an assumption.",
        "103": "When working through a customer-service request involving orders, exchanges, returns, or modifications, build a decision tree in which each branch of a customer's conditional preference (\"if A unavailable, then B, else only C\") is a distinct leaf tied to a concrete tool-query outcome. Before responding, trace which leaf the retrieved catalog or order data actually selects, and confirm that every enumerated sub-request maps to exactly one executed or declined action.",
        "104": "When handling a multi-part customer-service request, generate at least two distinct candidate resolution plans-one that fulfills the primary preference and one that triggers each stated fallback-then compare them side by side against retrieved tool evidence (catalog variants, order status, authentication data) to select the plan whose preconditions are actually satisfied. Reject any plan that relies on unverified inventory, cross-type exchanges, or modifications to non-pending orders, and surface gaps as direct questions rather than defaulting silently.",
        "105": "When routing a customer-service request that involves orders, exchanges, returns, modifications, or catalog lookups, first classify the request by counting distinct sub-tasks and flagging any conditional \"if X then Y\" branches, private-persona constraints, or missing authentication data. Route to an agent capable of strict policy adherence and batched tool use only when every sub-task, fallback branch, and verification step can be handled in one coordinated trajectory rather than piecemeal.",
        "106": "When routing a customer-service scenario that bundles multiple sub-requests, conditional preferences, or policy-sensitive actions (authentication, exchanges, returns, order modifications, catalog counts), formally prove routability by checking three conditions: every sub-task is individually specified, every conditional branch has a decidable trigger tied to retrievable evidence, and no action violates domain invariants like pending-only modifications or same-type exchanges. Only dispatch to an execution agent once all three conditions hold; otherwise route to a clarification step.",
        "107": "When routing a customer-service request that bundles authentication, catalog lookups, order modifications, exchanges, or returns, first draft two or more competing resolution trajectories-one assuming the customer's primary preference is satisfiable and one for each stated fallback-then select among them only after matching each trajectory's preconditions (pending status, variant availability, same-item-type rule, name+zip auth when email is unknown) against retrieved tool evidence. Route to clarification whenever any sub-request, branch trigger, or policy precondition remains unverified.",
        "108": "When orchestrating a customer-service request that combines identity verification, catalog queries, and order actions, produce a strictly formatted execution manifest listing each sub-task on its own numbered line with fields for action-type, required tool, policy preconditions, and the exact conditional trigger that selects it. Dispatch sub-tasks in a single batched plan only after every manifest row has verified evidence; otherwise emit a clarification line in the same format instead of partial execution.",
        "109": "When decomposing a customer-service request with multiple sub-tasks, formalize it as a dependency graph: nodes are atomic actions (authenticate, list-orders, query-catalog-variant, modify, exchange, return, count-items), edges encode precondition relations (e.g., authenticate precedes any order action; list-orders precedes filtering by pending status), and conditional preferences become guarded edges whose truth value is fixed by a specific tool result. Schedule independent nodes in parallel batches and serialize only along true dependencies, refusing to execute any node whose guard or precondition is not yet witnessed by retrieved evidence."
    },
    "verifier": {
        "100": "Before finalizing a response to a multi-part customer-service request, run a strict format-and-constraint checklist: confirm authentication was completed via name plus zip when email is unknown, verify every sub-request (including exact counts like \"how many tshirt options\") is addressed in one consolidated reply, and confirm each conditional fallback resolves to the branch supported by retrieved evidence. Reject any plan that modifies non-pending orders, exchanges across item types, or relies on unverified catalog variants, and emit a clarification question instead of partial execution.",
        "101": "When verifying a proposed resolution to a customer-service request involving orders, exchanges, returns, or modifications, perform a line-by-line audit that flags three specific error classes: unverified inventory claims (any variant asserted without a catalog query), silent defaulting (a conditional fallback whose trigger was never evaluated against retrieved evidence), and policy violations (cross-type exchanges, modifications to non-pending orders, or skipped name-plus-zip authentication). Block finalization until each flagged item is either corrected or converted into an explicit clarification question.",
        "102": "When auditing a proposed resolution to a retail customer-service trajectory, apply a counterfactual swap test: for every conditional preference (\"if A unavailable, then B, else only C\"), mentally flip the trigger and check whether the plan would change accordingly, exposing silent defaulting to the primary option. Also verify that each asserted product variant, order status, and item-type match is backed by a specific retrieved tool result, and block finalization until every unsupported claim becomes either a fresh query or a clarification question.",
        "103": "When verifying a retail customer-service resolution, construct a formal proof obligation for each claim: every asserted variant availability must cite a specific catalog query result, every order modification must cite a retrieved pending status, and every authentication must show name-plus-zip was used when email is unknown. Reject the trajectory if any obligation lacks a discharging tool result, and require the missing query or a clarification question before finalization.",
        "104": "When verifying a proposed resolution to a multi-part retail service request, apply a Hoare-triple audit to each planned action: write its precondition (e.g., order.status = pending, variant \u2208 catalog, auth = name+zip), the action itself, and its postcondition, then reject the step unless a specific tool result discharges the precondition. Additionally, enumerate every conditional branch as a disjoint case split and confirm exactly one case fires based on retrieved evidence before allowing finalization.",
        "105": "When verifying a proposed resolution to a customer-service trajectory involving authentication, orders, or catalog actions, apply a completeness-and-grounding check by cross-referencing the original request against the plan: every enumerated sub-task (including exact counts) must have a corresponding action, and every stated conditional preference must have its trigger evaluated against a concrete tool result rather than assumed. Before approving finalization, explicitly flag any sub-task that was silently dropped, any fallback branch whose selection lacks evidence, and any persona constraint (such as privacy or single-pass resolution) that the draft response fails to honor.",
        "106": "When verifying a retail customer-service trajectory, run an inversion test on each conditional preference by asking \"what tool evidence would flip this branch?\" and confirm the chosen action would differ accordingly; if the plan behaves identically under both branches, the fallback was silently ignored. Additionally, reframe every policy constraint as a falsifiable claim (e.g., \"this order is pending,\" \"this variant exists,\" \"these items share a type\") and demand a specific tool result as counter-evidence search before approval."
    },
    "answerer": {
        "100": "When responding to a multi-part customer-service request, format the final reply as a single consolidated message that explicitly addresses every sub-request in the order raised, cites concrete tool-retrieved values (exact counts, order IDs, variant specifications) rather than vague summaries, and names which conditional branch fired with its triggering evidence. Omit any personal details beyond what the customer volunteered, and convert unresolved preconditions into numbered clarification questions instead of partial execution.",
        "101": "When concluding a multi-part retail customer-service trajectory, present the final answer as one consolidated message that mirrors the customer's original ordering of sub-requests, embeds exact tool-retrieved values (counts, order IDs, variant codes, order statuses) inline, and explicitly names which conditional branch fired along with the evidence that triggered it. Convert any precondition still lacking tool confirmation into a numbered clarification question rather than executing partially, and reveal no personal data beyond what the customer volunteered.",
        "102": "When delivering the final output for a multi-part retail customer-service trajectory, assemble the reply using a scripted template that opens with an authentication confirmation line (name plus zip), followed by a bulleted list mirroring the customer's sub-requests in their original sequence, each bullet annotated with the triggering tool evidence and the resulting action or declination. Close with an explicit confirmation prompt before any irreversible execution, and convert unresolved preconditions into inline clarification questions rather than partial commitments.",
        "103": "When the final output is a customer-facing reply for a multi-part retail service request, structure it as a concise three-part message: an opening that confirms identity verification via name and zip, a middle section that addresses each sub-request in the customer's original order with inline tool-retrieved specifics (exact counts, order IDs, variant details, pending status) and names which conditional branch fired, and a closing that requests explicit confirmation before any irreversible action. Never invent inventory or personal details, and fold unresolved preconditions into numbered clarification questions rather than executing partially.",
        "104": "When aggregating multiple candidate resolutions for a multi-step retail service request, score each candidate on four weighted axes-authentication method used, completeness of sub-request coverage, correctness of conditional-branch selection against cited tool evidence, and policy compliance (pending-only modifications, same-type exchanges)-and select the candidate with the highest score. Break ties by preferring the candidate that consolidates all sub-requests into one reply and converts unverified preconditions into explicit clarification questions rather than silent assumptions.",
        "105": "When reconciling multiple candidate resolutions to a multi-part retail service request, apply a lexicographic tie-breaker in this order: reject any candidate that skips name-plus-zip authentication or violates pending-only or same-item-type policy, then prefer candidates whose conditional-branch selection is explicitly justified by cited tool evidence, then prefer candidates that address every enumerated sub-request (including exact counts) in one consolidated reply. Break remaining ties by choosing the candidate that converts unverified preconditions into clarification questions rather than silent defaults.",
        "106": "When aggregating candidate resolutions for a multi-part retail service request, run an assumption-violation probe on each candidate: ask what happens if the customer's preferred variant is actually out of stock, if the target order is delivered rather than pending, or if authentication data is stale, and discard any candidate whose plan would silently proceed under those violations. Prefer the candidate that explicitly cites tool evidence for each conditional branch trigger and converts unverifiable preconditions into clarification questions rather than defaults."
    }
}