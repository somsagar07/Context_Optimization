{
    "reasoner": {
        "100": "When handling a customer-service simulation describing a connectivity or service issue, first decompose the scenario into a formal decision tree whose root branches on location context (domestic versus roaming/abroad), then on diagnostic state (signal bars, roaming toggle, data balance), and finally on customer-imposed constraints (permitted remedies like data refuel up to the stated cap, forbidden remedies like plan changes). Before declaring resolution, verify that every acceptance predicate holds simultaneously, especially that the final speed-test outcome exactly equals the required tier and not a weaker one. Never substitute assumed tool outputs for actual tool calls, and re-invoke diagnostics after each remediation to confirm state transitions.",
        "101": "When the input describes a customer-support simulation with a stated resolution path, explicit acceptance criteria, and a list of permitted versus forbidden remedies, adopt a closed-loop diagnostic reasoning pattern: call a status or diagnostic tool, interpret its literal output, apply exactly one policy-permitted remediation, then re-poll to confirm the state actually changed. Continue this loop until the acceptance predicate is strictly satisfied, treating any weaker outcome as unresolved and never proposing remedies the customer has explicitly forbidden.",
        "102": "When reasoning about a scripted customer-service scenario whose target output is an abstract resolution directive, treat the problem as a proof obligation: explicitly enumerate the acceptance predicates from the stated criteria, the permitted-remedy set, and the forbidden-remedy set as formal premises before any action. Derive each proposed step as a lemma justified by a specific tool observation or stated policy, and only conclude resolution once a terminal diagnostic literally satisfies the strictest acceptance tier. Reject any inference that depends on assumed tool outputs, partial satisfaction of criteria, or remedies outside the permitted set.",
        "103": "When handling a scripted support-agent simulation where the target output is a generic resolution directive, recognize that the evaluation hinges on trajectory fidelity rather than answer content, so produce that standard directive verbatim while internally planning a tool-grounded dialogue. Map the customer's location, constraints, and acceptance threshold to a branching troubleshooting script, and ensure every simulated action cites a concrete diagnostic call rather than narrated assumptions. Treat any remedy the customer forbids as strictly unavailable, even when it would resolve the issue faster.",
        "104": "When reasoning about a support-agent simulation with stated acceptance criteria and permitted/forbidden remedies, run an explicit error-detection pass before committing to any action: list the assumptions your proposed step depends on (current signal state, roaming status, remaining data balance, location branch) and mark each as either verified by a specific diagnostic tool call or unverified. If any assumption is unverified, issue the corresponding diagnostic first rather than proceeding, and never accept a near-miss outcome as satisfying a strict acceptance tier.",
        "105": "When the input is a scripted support-agent scenario with a target resolution directive, route it to a tool-grounded dialogue planner rather than a free-form answer generator, and first audit the scenario for contradictions between the stated resolution path, the customer's location branch, and the permitted-versus-forbidden remedy sets. If any acceptance predicate, location cue, or constraint is ambiguous, flag it and require a diagnostic tool call before committing to a remediation branch.",
        "106": "When routing a scripted support-agent scenario whose nominal output is a generic resolution directive, apply inverse-problem reasoning by working backward from the strictest acceptance predicate to identify the minimal causal chain of tool-grounded actions that could produce it. Creatively enumerate non-obvious root causes (roaming toggles, depleted balances, tower state, APN config) ranked by how well they explain the customer's location and symptom pair, and route to the branch whose causal hypothesis best fits before committing to any remediation.",
        "107": "When routing a scripted customer-support scenario that specifies a resolution path, customer location, acceptance criteria, and permitted versus forbidden remedies, classify it as a tool-use dialogue task and dispatch it to a didactic, step-by-step troubleshooting agent rather than a single-shot answerer. Direct that agent to teach its reasoning by naming each diagnostic tool it would invoke, explaining what each output rules in or out, and halting only when the strictest acceptance tier is literally met.",
        "108": "When orchestrating a scripted support-agent scenario, generate at least two candidate decomposition plans-one organized by diagnostic phase (verify identity, probe state, remediate, reconfirm) and one organized by causal hypothesis branches (roaming misconfiguration, depleted balance, tower or APN fault)-then compare them against the stated acceptance tier and permitted-remedy set before committing. Select the plan whose sub-tasks each terminate in a concrete tool call and whose final sub-task re-polls the strictest acceptance predicate.",
        "109": "When orchestrating a scripted support-agent scenario, decompose it into a dependency graph where identity-verification and location-classification sub-tasks must complete before any remediation sub-task is dispatched, and where each remediation node has a mandatory downstream re-diagnostic node that must confirm the strictest acceptance tier. Stress-test the graph by asking what happens if the location cue is wrong, the permitted-remedy cap is exhausted, or a diagnostic returns stale data, and insert fallback branches rather than letting any path terminate on a near-miss outcome."
    },
    "verifier": {
        "100": "When verifying a proposed resolution for a scripted support-agent scenario with acceptance criteria and permitted/forbidden remedies, audit it as a formal proof by checking three theorems in order: (1) every premise about device or account state is justified by a cited diagnostic tool output rather than narration, (2) no step invokes a forbidden remedy or exceeds a stated cap, and (3) the terminal observation literally satisfies the strictest acceptance tier. Reject the trajectory if any theorem fails and demand the specific missing tool call.",
        "101": "When critiquing a proposed response to a scripted support-agent scenario, teach the reviewer to trace each claim back to its source by asking \"which tool call produced this state?\" for every assertion about device, network, or account status, and flagging any narrated outcome that lacks a cited diagnostic. Then verify that no forbidden remedy appears, no permitted cap is exceeded, and the closing observation exactly matches the strictest acceptance tier rather than a nearby one.",
        "102": "When reviewing a proposed trajectory for a scripted customer-support simulation with a strict acceptance tier and a permitted-versus-forbidden remedy list, run an adversarial verification pass that separately challenges four claims: the location branch is correctly identified, each state assertion is backed by a cited diagnostic call rather than narration, no step uses a forbidden remedy or exceeds a stated cap, and the terminal diagnostic literally matches the strictest tier. Reject the trajectory and name the specific missing or misused tool call whenever any challenge fails.",
        "103": "Before finalizing any response to a scripted support-agent scenario, enforce a strict output-format audit: confirm that the final reply exactly reproduces the required resolution directive verbatim when that is the expected output, with no paraphrasing, added commentary, or embedded narration of tool calls. Then verify the planned trajectory respects every stated cap, forbids every proscribed remedy, and terminates only when the strictest acceptance tier is literally matched, rejecting any near-miss wording.",
        "104": "When verifying a proposed trajectory for a scripted support-agent scenario, generate at least two alternative resolution paths branching on the most ambiguous cue (typically location or root-cause hypothesis) and compare them side by side against the acceptance predicates, permitted-remedy caps, and forbidden-remedy list. Commit only to the path whose every state claim cites a specific diagnostic call and whose terminal observation exactly matches the strictest acceptance tier, and surface the discarded path's failure reason as the critique.",
        "105": "When verifying a proposed trajectory for a scripted support-agent scenario, run a counterfactual falsification pass by asking for each remediation step \"what observation would prove this step failed?\" and require that the corresponding diagnostic tool was actually re-invoked afterward to rule out that failure. Flag any step whose success is inferred from narration, any acceptance claim resting on a weaker tier than required, and any assumption about location, roaming state, or balance that lacks a cited tool output.",
        "106": "When verifying a proposed trajectory for a scripted support-agent dialogue, perform a sensitivity analysis on each diagnostic: ask whether a one-step change in the cited tool output (signal bar count, roaming flag, remaining balance, speed tier) would alter the chosen remediation branch, and require that such pivotal observations come from a fresh tool call rather than stale or narrated state. Reject trajectories where the terminal acceptance check reads an earlier snapshot instead of a post-remediation re-poll."
    },
    "answerer": {
        "100": "When the input is a scripted support-agent scenario whose expected output is an abstract resolution directive, present your final answer as that exact directive copied verbatim with no quotation marks, no added explanation, and no inline summary of the troubleshooting steps. Before emitting it, confirm the scenario matches this pattern by checking for a stated resolution path, acceptance criteria, and permitted-versus-forbidden remedies, and suppress any impulse to narrate diagnostics or propose remedies in the visible answer.",
        "101": "When the input is a scripted customer-support simulation that states a resolution path, customer context, acceptance criteria, and permitted-versus-forbidden remedies, output the generic resolution directive verbatim as a single plain-text line, with no quotation marks, no bullet points, no tool-call narration, and no mention of the customer's specific issue or location. Before emitting, confirm the scenario fits this pattern and strip any drafted troubleshooting commentary from the visible answer.",
        "102": "When the input describes a scripted service-desk simulation specifying a resolution path, customer context, acceptance thresholds, and allowed versus disallowed remedies, render the final answer as the canonical resolution directive on a single unquoted plain-text line, matching the target phrasing word-for-word. Strip any draft troubleshooting steps, tool traces, location details, or customer-specific wording before emitting, and never append explanations, headings, or trailing punctuation beyond the sentence's own period.",
        "103": "When a scripted support-agent scenario's target output is a generic resolution directive, emit that directive as one plain-text line matching the canonical phrasing exactly, then cross-check your draft against three red flags before sending: any verbatim echo of the customer's symptom or location, any simulated tool call or diagnostic narration, and any added qualifiers, quotes, or headings. Delete anything that trips these flags so only the canonical sentence remains.",
        "104": "When aggregating multiple candidate answers for a scripted support-agent scenario whose expected output is an abstract resolution directive, disregard any candidate that paraphrases the canonical sentence, narrates diagnostics, echoes the customer's symptom or location, or adds quotes, headings, or extra punctuation. Select the candidate that reproduces the directive verbatim on a single plain-text line, and if none qualifies, synthesize one from the closest match by stripping all deviations.",
        "105": "When aggregating multiple candidate resolutions for a scripted support-agent scenario that states a resolution path, customer context, acceptance criteria, and permitted-versus-forbidden remedies, score each candidate on four binary axes: exact verbatim match to the canonical directive, absence of customer-specific echoes, absence of diagnostic narration, and absence of added formatting such as quotes or headings. Select only a candidate that scores perfectly on all four, and if none qualifies, construct one by stripping the highest-scoring candidate down to the canonical sentence alone.",
        "106": "When aggregating candidate trajectories for a scripted support-agent simulation, run a conflict-resolution pass that tags each disagreement as either a surface-format divergence (wording, punctuation, quoting of the canonical directive) or a substantive branch divergence (location classification, chosen remedy, acceptance tier reached). Resolve format conflicts by enforcing the exact canonical sentence, and resolve substantive conflicts by selecting the branch whose every state claim cites a fresh diagnostic tool output and whose terminal re-poll literally matches the strictest acceptance tier."
    }
}